# springboot2-crud-codegen

English | [简体中文](README.zh-CN.md)

`springboot2-crud-codegen` is an advanced Python CLI that generates enterprise-grade Spring Boot 2 + MyBatis-Plus CRUD projects from JSON configuration.

It is designed for fast scaffolding of admin-style backends and goes beyond simple CRUD by generating built-in **RBAC Security**, **Multi-tenancy**, **Excel Exports**, **MockMvc Unit Tests**, and a fully functional standalone Vue 2 admin frontend.

## What It Generates

- Full Maven project structure
- Spring Boot 2.x application with Java 8 target
- Spring Security + JWT authentication with RBAC (Role-Based Access Control)
- MyBatis-Plus CRUD for single tables with Multi-tenancy interceptors
- Relation query endpoints with generated join SQL and logical delete filtering
- EasyExcel export endpoints for table data
- Auto-generated MockMvc unit tests for all controllers
- Request DTO validation annotations
- Unified `Result<T>` and `PageResult<T>` response wrappers
- Local file upload endpoint (`FileController`) and static resource mapping
- `application.yml` with environment-variable datasource placeholders
- `init.sql` with database creation, implicit RBAC system tables (`sys_user`, etc.), explicit/inferred constraints, and seed data
- Optional standalone Vue 2 + Element UI admin frontend (with login page and Axios interceptors)

## Supported Features

- **Query & Relations**: `EQ`, `NE`, `LIKE`, `GT`, `GE`, `LT`, `LE` operators for tables and left/inner joins.
- **Security**: Built-in JWT generation/validation, `@PreAuthorize` method security, automatic injection of 5 standard RBAC tables.
- **Multi-tenancy**: Request-level tenant isolation using `TenantLineInnerInterceptor` and `X-Tenant-Id` or JWT claims.
- **Data Export**: Built-in Alibaba EasyExcel integration generating `XxxExportDto` and `/export` APIs.
- **File Upload**: Native multipart file upload to local directories with Vue `el-upload` integration.
- **Testing**: Autogenerated `@WebMvcTest` controller tests mocking the service layer.
- **Sorting**: Whitelist-based sorting for single-table and relation queries.
- **Frontend Generation**: Vue 2 admin with dynamic routing, dictionary lookups, `image-upload` components, and locale switching (`zh-CN` / `en-US`).

## Install

```bash
python -m pip install -e .
```

## Quick Start

Generate a project with security enabled:

```bash
python -m codegen -c examples/sample_security.json -o /tmp/codegen-out
```

Generated root:

```text
/tmp/codegen-out/<artifactId>/
```

Common output files:

- `pom.xml` (includes Spring Security, JJWT, EasyExcel, etc.)
- `src/main/resources/application.yml`
- `src/main/resources/init.sql`
- `src/main/java/<basePackage>/security/*` (Jwt filters, UserDetailsService)
- `src/main/java/<basePackage>/controller/*` (With `@PreAuthorize` and `/export`)
- `src/test/java/<basePackage>/controller/*Test.java` (MockMvc tests)
- `frontend/src/views/login/index.vue`

## Configuration Rule Highlights

### Security (`security`)

Enable RBAC and JWT by defining the `security` block at the top level:

```json
"security": {
  "enabled": true,
  "jwt": {
    "secret": "my-super-secret-key-that-is-very-long",
    "expiration": 86400,
    "header": "Authorization",
    "prefix": "Bearer "
  },
  "rbac": {
    "superAdminRole": "ROLE_ADMIN"
  }
}
```
When enabled, the parser implicitly injects `sys_user`, `sys_role`, `sys_user_role`, `sys_menu_permission`, and `sys_role_permission` into your table list, complete with initial seed data.

You can protect your tables by adding the `auth` object:

```json
"tables": [
  {
    "name": "orders",
    "auth": {
      "roles": ["ADMIN", "MANAGER"],
      "permissions": {
        "query": "order:view",
        "create": "order:add",
        "update": "order:edit",
        "delete": "order:delete"
      }
    }
  }
]
```

### Multi-tenancy (`global.tenant`)

Enable data isolation by adding tenant config to `global`:

```json
"global": {
  "tenant": {
    "enabled": true,
    "column": "tenant_id"
  }
}
```

### File Uploads (`backend.uploadDir`)

Set `uploadDir` in the backend block to control where files are saved locally. Use `image-upload` or `file-upload` in frontend component definitions to automatically generate Element UI upload widgets wired to the backend API.

## Build and Test Commands

Install editable package:

```bash
python -m pip install -e .
```

Run Python tests:

```bash
python -m unittest discover -s tests -v
```

Syntax check:

```bash
python -m compileall codegen tests
```

Compile a generated Java project:

```bash
mvn package -DskipTests
```

## Current Scope and Limits

- Java 8 only
- Spring Boot 2.x only
- MySQL-oriented datasource and SQL output
- Frontend generation currently targets Vue 2 + Element UI only

## License

Currently no explicit license file is included in this repository.